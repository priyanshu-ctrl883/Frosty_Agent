E2E_BASE_URL=http://localhost:3000
# Prefer apps/merchant-dashboard/e2e/.env for Playwright credentials.
# .env.local also works now (Playwright loads it), but keep secrets out of chat/commits.
# E2E_EMAIL=your-merchant@example.com
# E2E_PASSWORD=your-password
