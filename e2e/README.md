# Moved

Playwright UI tests, config, catalog, and QA runner now live in:

**`scripts/qa/ui_playwright/`**

Run from this app:

```bash
npm run test:e2e
```

Or from the QA panel → **UI - Playwright (SET + smoke)**.
